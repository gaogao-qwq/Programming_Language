#include <stdio.h>
#include <string.h>

int main(){
    int num[10] = {29, 65, 75, 80, 4, 65, 42, 11, 45, 73};
    int min[10];
    min[0] = num[0];
    for (size_t i = 1; i < 10; i++)
    {
        if (num[i] < min[0])
        {
            
        }
        
    }
    
    return 0;
}